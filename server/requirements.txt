# Crumbs attribution ledger — server dependencies (local MVP v0.1)
# Pinning is loose here for development. The DEPLOYMENT manifest is
# server/requirements.lock — exact pins with SHA-256 hashes, generated with:
#   uv pip compile requirements.txt --generate-hashes -o requirements.lock
# Install it reproducibly with:
#   uv pip install --require-hashes -r requirements.lock
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
sqlalchemy>=2.0,<3
redis>=5.0,<6
httpx>=0.27,<1
pytest>=8.2,<9
alembic>=1.13,<2
